#ifndef H_TYPES_H
#define H_TYPES_H

#include <stddef.h>
#include <stdbool.h>

/** Immutable index type (default) */
typedef const size_t ind_t;

/** Mutable index type (choice) */
typedef size_t mind_t;

/** Data type */
typedef double data_t;


#endif // H_TYPES_H
